{
  "name": "gestorapp-functions",
  "description": "GestorApp Firebase Cloud Functions",
  "scripts": {
    "build":  "tsc",
    "serve":  "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "logs":   "firebase functions:log"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "axios":            "^1.7.2",
    "firebase-admin":   "^12.3.1",
    "firebase-functions": "^6.0.1"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0"
  },
  "private": true
}